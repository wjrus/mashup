<% content_for :title, "Staff" %>

<div class="page-header">
  <div>
    <p class="eyebrow">Administration</p>
    <h1>Staff Accounts</h1>
  </div>
</div>

<div class="table-wrap">
  <table>
    <caption class="sr-only">Staff accounts</caption>
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Email</th>
        <th scope="col">Access</th>
        <th scope="col">Last sign-in</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <th scope="row"><%= user.name.presence || user.email %></th>
          <td><%= user.email %></td>
          <td><span class="pill"><%= user.role.humanize %></span></td>
          <td><%= user.last_sign_in_at&.to_fs(:long) || "Never" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
