<div class="nested-form" data-nested-form-row data-new-record="<%= run_fields.object.new_record? %>">
  <%= run_fields.hidden_field :_destroy, value: "0", data: { destroy_field: true } %>

  <div>
    <%= run_fields.label :space_id, "Space" %>
    <%= run_fields.collection_select :space_id, Space.where(active: true).order(:name), :id, :name, { include_blank: "Choose a space" } %>
  </div>
  <div>
    <%= run_fields.label :starts_at %>
    <%= run_fields.datetime_field :starts_at %>
  </div>
  <div>
    <%= run_fields.label :ends_at %>
    <%= run_fields.datetime_field :ends_at %>
  </div>
  <div>
    <%= run_fields.label :status %>
    <%= run_fields.select :status, BookingRun.statuses.keys.map { |key| [key.humanize, key] } %>
  </div>
  <button type="button"
          class="secondary-button"
          data-action="nested-form#remove"
          data-confirm-title="Remove run?"
          data-confirm-message="Remove this scheduled run from the booking? The removal is applied when you save."
          data-confirm-label="Remove run">Remove run</button>
</div>
